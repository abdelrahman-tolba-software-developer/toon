name: toon_formater
description: Token-Oriented Object Notation (TOON) – A compact, deterministic JSON format for LLM prompts (Dart implementation)
version: 1.0.2
homepage: https://toonformat.dev
repository: https://github.com/abdelrahman-tolba-software-developer/toon/tree/main/packages/toon_formater

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.11.0
  # Build-time dependencies (used by code generators in lib/)
  # Note: These versions are kept compatible to ensure code generator works correctly
  build: ^2.4.0
  source_gen: ^1.5.0
  analyzer: ^6.0.0

dev_dependencies:
  build_runner: ^2.4.0
  test: ^1.24.0
  lints: ^6.0.0
  benchmark_harness: ^2.2.2

